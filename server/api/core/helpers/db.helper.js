import config from '../../../config/config';

let pool = null;
let connection = null;

export const request = async () => {
  //
};

export const initDB = () => {
  // pool = new sql.ConnectionPool(config.db)
};
